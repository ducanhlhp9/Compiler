program TestRepeat; 
var sum : float;
    number : integer;
    x : integer;

Function F(n : Integer) : Integer;
  Begin
    If n = 0 Then F := 1 Else F := N * F (N - 1);
    n := If n >= 0 then n else -n;
  End;     

begin
    repeat 
        begin 
        sum, number := 5.5 + 5, 6;
        sum := sum + number;
        number := number - 2;
        end
    until 
        number = 0;
end.
